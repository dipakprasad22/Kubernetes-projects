apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: panelpulse
  namespace: panelpulse
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /ingest
            pathType: Prefix
            backend: { service: { name: collector, port: { number: 80 } } }
